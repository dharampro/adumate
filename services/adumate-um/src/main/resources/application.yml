server:
  port: 9000
  servlet:
    context-path: "/um"

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /api-doc-ui.html
  spring:
    data:
      mongodb:
        host: localhost
        port: 27017
        database: adumate
  debug: true
